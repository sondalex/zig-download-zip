# To do

- [ ] Implement custom extract function:
      Should support override (introspect filenames in zip file)
- [ ] Implement explicit calling → I.e. `zig build dz`
      This would avoid downloading assets each time.

